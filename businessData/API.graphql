query searchLocations($query: String!){
    searchLocations(query: $query){
        edges{
            node {
                name
                accountId
                city
                countryIso
                ownerEmail
            }
        }
    }
}

